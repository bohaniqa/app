enum BOQShiftInstruction {
  
  test,

  createMintAuthority,
  initializeMintAuthority,
  setMintAuthority,
  
  createEmployer,
  initializeEmployer,
  
  createEmployee,
  initializeEmployee,
  
  createShift,
  initializeShift,
  shift,
}