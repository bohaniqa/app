import 'package:flutter/material.dart';

class BOQ404Screen extends StatelessWidget {

  const BOQ404Screen({super.key});

  @override
  Widget build(final BuildContext context) {
    return const Center(
      child: Text("404 Page Not Found"),
    );
  }
}